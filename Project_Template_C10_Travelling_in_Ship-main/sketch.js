var s
function preload(){
seaImg.loadImage("sea.png")
shipImg.loadAnimation
}

function setup(){
  createCanvas(400,400);
  s.addImage(seaImg)
}

function draw() {
  background("blue");
 if(s.x<0){
sea.x=width/2
 }
}